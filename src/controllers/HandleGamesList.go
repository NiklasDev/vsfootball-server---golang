package controllers

import (
	// "encoding/json"
	// "fmt"
	// "github.com/gorilla/mux"
	// "jsonOutputs"
	// "models"
	"net/http"
)

func HandleGamesList(res http.ResponseWriter, request *http.Request) {
	// vars := mux.Vars(request)
	// guid := vars["guid"]
	// success, message, results, status := models.GamesList(guid)
	// outputJson, _ := json.Marshal(jsonOutputs.GameListOutput{success, message, results, status})

	// fmt.Fprintf(res, string(outputJson))
}
